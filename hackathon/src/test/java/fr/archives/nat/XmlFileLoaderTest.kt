package fr.archives.nat

import fr.archives.nat.xml.ead.sia.Ead
import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

/**
 * @author Patrick Allain - 12/7/18.
 */
internal class XmlFileLoaderTest {

	/*
	
    @Test
    fun `Should load the xml files`() {
        // GIVEN
        val xml = XmlFileLoader.XmlFiles.FRAN_IR_056040
        val loader = XmlFileLoader(xml, Ead::class)

        // WHEN
        val ead = loader.run()

        // THEN
        assertThat(ead).isNotNull
        assertThat(ead.eadheader.eadid.getvalue()).isEqualTo(xml.name)
    }
  	*/
}